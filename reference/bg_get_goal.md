# Get the active inferential goal for a scope

Get the active inferential goal for a scope

## Usage

``` r
bg_get_goal(project, scope)
```

## Arguments

- project:

  A `bg_handle`.

- scope:

  A scope string.

## Value

A goal record or `NULL`.
