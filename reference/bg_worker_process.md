# The internal worker process that executes a single node

The internal worker process that executes a single node

## Usage

``` r
bg_worker_process(args)
```

## Arguments

- args:

  List of worker arguments.
