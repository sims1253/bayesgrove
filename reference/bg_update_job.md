# Update an existing job record

Update an existing job record

## Usage

``` r
bg_update_job(project, job_id, ...)
```

## Arguments

- project:

  A `bg_handle`.

- job_id:

  The ID of the job to update.

- ...:

  Fields to update.

## Value

The updated job record.
