# Write a debug log entry for a worker job

Write a debug log entry for a worker job

## Usage

``` r
bg_worker_log(job_id, message, project_path)
```

## Arguments

- job_id:

  Character string identifying the job.

- message:

  Character string to write.

- project_path:

  Character string path to the project root.
