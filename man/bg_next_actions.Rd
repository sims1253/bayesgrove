% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow-protocol.R
\name{bg_next_actions}
\alias{bg_next_actions}
\title{Compute deterministic next workflow actions}
\usage{
bg_next_actions(project, scope = c("project", "branch"), branch_id = NULL)
}
\arguments{
\item{project}{A \code{bg_handle}.}

\item{scope}{One of \code{project} or \code{branch}.}

\item{branch_id}{Optional branch id, required for branch-scoped queries.}
}
\value{
A \code{bg_next_actions_result} plain-data list with \code{context},
\code{obligations}, \code{actions}, and \code{metadata$external_holds}.
}
\description{
Evaluates the active workflow packs against a derived workflow context and
returns obligations, suggested actions, and planner-ready external holds.
Callers can pass \code{result$metadata$external_holds} into \code{\link[=bg_plan]{bg_plan()}} to keep
workflow holds distinct from structural blockers.
}
\details{
For the built-in \code{bayesgrove.default_bayesian} pack, the returned
obligations and actions can include:
\itemize{
\item \code{review_computation_validity} and matching \code{computation_review} actions
for fresh warning/error summaries,
\item \code{review_fit_criticism} plus \code{fit_criticism} and \code{branch_and_modify}
actions for branch-scoped fit or diagnostic problems,
\item \code{compare_candidate_branches} plus comparison-node creation or
\code{model_comparison} decision actions when multiple clean fit candidates
exist, and
\item \code{accept_or_reject_branch} plus \code{branch_disposition} actions after a
current comparison exists for an active candidate set.
}

When the optional packs are active, the result can also include
prior-rationale recording, prior and posterior predictive review,
simulation-based calibration review, model-selection review keyed to
\code{model_comparison} and \code{stacking_weights} summaries, causal-question
prompts, and PAD annotation review.
}
