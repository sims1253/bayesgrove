% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{bg_open}
\alias{bg_open}
\title{Open a bayesgrove Project}
\usage{
bg_open(path = ".", readonly = FALSE, force = FALSE)
}
\arguments{
\item{path}{Path to the project directory.}

\item{readonly}{Whether to open the project in read-only mode.}

\item{force}{Logical. If \code{TRUE} and the project is locked, steal the lock
(use when the prior holder is known to be stale). Ignored when
\code{readonly = TRUE}.}
}
\value{
A \code{bg_handle}.
}
\description{
Opens an existing project. Opening a project never runs project-supplied
code: persisted node-kind executors are restored structurally only (the
kind name, contracts, and stored executor source kept as inert metadata).
To restore executable executors from the persisted source, call
\code{\link[=bg_restore_executors]{bg_restore_executors()}} with \code{trust = TRUE} after opening, or
re-register them with \code{\link[=bg_register_node_kind]{bg_register_node_kind()}}.
}
\details{
A non-readonly open acquires a single-writer lock
(\code{.bayesgrove/project.lock}) so two writers cannot mutate a project
concurrently. If the lock is already held, the holder's pid/host is
reported; pass \code{force = TRUE} to steal a stale lock. Readonly opens skip
locking and can run alongside a writer.
}
