% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decisions.R
\name{bg_repair_orphan_gates}
\alias{bg_repair_orphan_gates}
\title{Remove orphan gates that cannot be answered or bypassed}
\usage{
bg_repair_orphan_gates(project)
}
\arguments{
\item{project}{A \code{bg_handle}.}
}
\value{
Invisible named list keyed by gate ID with \code{gate_id}, \code{edge_id},
\code{from_node_id}, and \code{to_node_id} entries for every removed gate, empty
when there was nothing to repair.
}
\description{
Drops pending graph gates that have no gate spec — the unanswerable state
an interrupted \code{bg_add_gate()} from an older version could leave behind —
and commits the cleaned graph, so recovery no longer requires
hand-editing \code{graph.json}. Gates on edges with retired or disabled
endpoints are left alone, matching what \code{bg_plan()} and \code{bg_status()}
report as anomalies. A project without orphan gates is left untouched.
}
