% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/26-serve.R
\name{bg_serve}
\alias{bg_serve}
\title{Start the experimental local IPC server}
\usage{
bg_serve(project, host = "127.0.0.1", port = NULL, poll_interval = 0.2)
}
\arguments{
\item{project}{A \code{bg_handle}.}

\item{host}{Host interface to bind (default \code{"127.0.0.1"}).}

\item{port}{TCP port to bind. If \code{NULL}, a random high port is selected.
Explicit ports must be whole numbers between 1 and 65535.}

\item{poll_interval}{Poll interval in seconds for detecting out-of-band state
changes, meaning updates made by other sessions or external processes
outside the current websocket request/response path.}
}
\value{
A \code{bg_server} handle with \code{url}, \code{host}, \code{port},
\code{protocol_version}, \code{service()}, and \code{stop()} members. Call
\code{service(timeout = 0.05)} to process pending server events inside the
current R process; it delegates to \code{httpuv::service()} and invisibly
returns \code{TRUE}. Call \code{stop()} for a synchronous shutdown that closes the
listener, drops connected clients, and invalidates future polling work.
}
\description{
Starts a local WebSocket server that exposes the bounded remote IPC contract
for external clients. On connection the server emits a full
\code{GraphSnapshot} message, then streams lightweight \code{ProtocolEvent} updates as
workflow state changes. The exposed command surface is limited to the
query/update operations marked \code{remote_accessible} in \code{bg_api_boundary()};
lifecycle management, unrestricted filesystem access, and other non-remote
package entry points remain outside the protocol.
}
\details{
Heavy execution should be coordinated through existing async pathways, not by
running long synchronous handlers in the foreground protocol thread. Remote
submission therefore routes async execution through \code{mirai}.
}
\examples{
\dontrun{
project <- bg_init(tempfile("bayesgrove-server-"))
server <- bg_serve(project)
server$url
server$service()
server$stop()
}
}
