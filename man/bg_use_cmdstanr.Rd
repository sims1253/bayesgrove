% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backends-cmdstanr.R
\name{bg_use_cmdstanr}
\alias{bg_use_cmdstanr}
\title{Register cmdstanr node kinds on a project.}
\usage{
bg_use_cmdstanr(project)
}
\arguments{
\item{project}{A \code{bg_handle}.}
}
\value{
Invisibly, the project handle.
}
\description{
Registers the built-in cmdstanr executor node kinds: \code{stan_data},
\code{cmdstanr_fit}, \code{prior_fit}, \code{loo}, \code{compare}, \code{ppc}, \code{loo_pit}, and \code{sbc}.
Each executor is a built-in resolved by reference (no source text
persisted). The fit executors compute HMC diagnostics themselves and emit
\code{hmc_diagnostics}
summaries with severity rules; no user-written diagnostic code is required.
}
\details{
The \code{ppc} kind's posterior-predictive p-values are conservative tripwires:
an extreme value flags gross misfit, but an unremarkable value is weak
evidence that the model is adequate. Treat the graphical check via
\code{\link[=bg_plot]{bg_plot()}} as the primary posterior-predictive diagnostic and the
p-values as its alarm layer.
}
