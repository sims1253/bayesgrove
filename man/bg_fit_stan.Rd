% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sugar.R
\name{bg_fit_stan}
\alias{bg_fit_stan}
\title{Fit a Stan model in one call}
\usage{
bg_fit_stan(handle, stan_file, data, label = NULL, ...)
}
\arguments{
\item{handle}{A \code{bg_handle}.}

\item{stan_file}{Path to the Stan model file.}

\item{data}{A named list (or environment) of Stan data.}

\item{label}{Optional label for the fit node.}

\item{...}{Passed to the fit node as params (e.g. \code{chains = 4}).}
}
\value{
The fit node id, invisibly. The run handle is printed.
}
\description{
Creates a data node (via \link{bg_set_node_data}), a \code{cmdstanr_fit} node consuming
it, dispatches \link{bg_run}, and returns the fit node id invisibly while printing
the run handle. Pure sugar over the existing verbs — no new semantics. Extra
arguments in \code{...} become the fit node's params (e.g. \code{chains}, \code{seed},
\code{iter_warmup}, \code{iter_sampling}).
}
\details{
Call \code{\link[=bg_use_cmdstanr]{bg_use_cmdstanr()}} first so the \code{cmdstanr_fit} kind is registered.
}
