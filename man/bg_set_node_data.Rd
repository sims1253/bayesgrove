% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{bg_set_node_data}
\alias{bg_set_node_data}
\title{Attach a data object to a node via the content-addressed store}
\usage{
bg_set_node_data(project, node_id, data)
}
\arguments{
\item{project}{A \code{bg_handle}.}

\item{node_id}{The node to attach data to (typically a \code{stan_data} node).}

\item{data}{An R object to attach.}
}
\value{
The node ID, invisibly.
}
\description{
Serializes \code{data} through the project's CAS store and records a
\code{data_ref = "cas:sha256:..."} param on the node, so the \code{stan_data} executor
can fetch it at run time without an embedded code channel. Existing params
are preserved (merged, not replaced). The data object must be serializable
via \code{\link[base:saveRDS]{base::saveRDS()}}.
}
