% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{bg_update_node}
\alias{bg_update_node}
\title{Update a node in the bayesgrove project graph}
\usage{
bg_update_node(
  project,
  node_id,
  label = NULL,
  params = NULL,
  metadata = NULL,
  replace = FALSE
)
}
\arguments{
\item{project}{A \code{bg_handle}.}

\item{node_id}{The node ID to update.}

\item{label}{Optional new label.}

\item{params}{Optional new parameters (merged by default).}

\item{metadata}{Optional new metadata (merged by default).}

\item{replace}{Logical scalar, default \code{FALSE}. When \code{TRUE}, \code{params} and
\code{metadata} REPLACE the existing lists wholesale instead of merging.}
}
\value{
The node ID.
}
\description{
\code{params} and \code{metadata} default to merging into the node's existing values
via \link[utils:modifyList]{utils::modifyList}, so \code{bg_update_node(handle, id, params = list(stan_file = f))}
updates just \code{stan_file} and preserves \code{chains}, \code{seed}, etc. Pass
\code{replace = TRUE} to replace the existing lists.
}
