% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sugar.R
\name{bg_fit_brms}
\alias{bg_fit_brms}
\title{Fit a brms model in one call}
\usage{
bg_fit_brms(handle, formula, data, label = NULL, ...)
}
\arguments{
\item{handle}{A \code{bg_handle}.}

\item{formula}{A \code{brmsformula} or formula object.}

\item{data}{A data frame of observations.}

\item{label}{Optional label for the fit node.}

\item{...}{Passed to the fit node as params (e.g. \code{chains = 4}).}
}
\value{
The fit node id, invisibly. The run handle is printed.
}
\description{
Creates a data node (via \link{bg_set_node_data}), a \code{brms_fit} node consuming
it, dispatches \link{bg_run}, and returns the fit node id invisibly while printing
the run handle. Extra arguments in \code{...}
become the fit node's params (e.g. \code{chains}, \code{seed}, \code{iter}).
}
\details{
Call \code{\link[=bg_use_brms]{bg_use_brms()}} first so the \code{brms_fit} kind is registered.
}
