% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fingerprint.R
\name{bg_compute_fingerprint}
\alias{bg_compute_fingerprint}
\title{Compute the cache fingerprint for a node}
\usage{
bg_compute_fingerprint(
  project,
  node_id,
  upstream_fingerprints = list(),
  environment_manifest = list(),
  graph = NULL
)
}
\arguments{
\item{project}{A \code{bg_handle}.}

\item{node_id}{The ID of the node to fingerprint.}

\item{upstream_fingerprints}{A named list mapping upstream node IDs to their computed fingerprints.}

\item{environment_manifest}{Optional list of package versions. When empty,
a default manifest (R + bayesgrove version) is used so that reinstalling
a new package version invalidates caches.}

\item{graph}{Optional pre-read graph. If \code{NULL} the graph is read from disk.}
}
\value{
A SHA-256 fingerprint string.
}
\description{
The fingerprint is a SHA-256 over a deterministic JSON payload combining:
the serialization format version, the node kind, normalized params, ordered
upstream fingerprints, an environment manifest (R and key package
versions), the registered executor, and any backend signature.
}
