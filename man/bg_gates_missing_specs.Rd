% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decisions.R
\name{bg_gates_missing_specs}
\alias{bg_gates_missing_specs}
\title{Find pending graph gates that have no matching gate spec}
\usage{
bg_gates_missing_specs(project, graph = NULL, specs = NULL)
}
\arguments{
\item{project}{A \code{bg_handle}.}

\item{graph}{Optional already-loaded raw graph.}

\item{specs}{Optional already-read gate specs.}
}
\value{
A named list keyed by gate ID with \code{gate_id}, \code{edge_id},
\code{from_node_id}, and \code{to_node_id} entries, empty when every pending
graph gate has a spec.
}
\description{
\code{bg_add_gate()} writes the structural gate and the semantic spec as two
commits; a failure in between leaves a gate that blocks its downstream
node but cannot be answered. This reports that state so \code{bg_plan()} and
\code{bg_status()} can surface it instead of hiding the blockage.
}
\keyword{internal}
