% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{bg_restore_executors}
\alias{bg_restore_executors}
\title{Restore user-supplied executors from persisted source.}
\usage{
bg_restore_executors(project, trust = FALSE)
}
\arguments{
\item{project}{A \code{bg_handle}.}

\item{trust}{Logical. If \code{FALSE} (default), print sources and abort without
evaluating. If \code{TRUE}, evaluate the sources.}
}
\value{
Invisibly, the \code{project} handle with executors populated.
}
\description{
Opening a project never runs project-supplied code. This function is the
explicit, opt-in path to restore executors stored as source text in the
project's runtime manifest.
}
\details{
With \code{trust = FALSE} (the default) the stored source for each kind is
printed and the function aborts, so the user can review what would be
evaluated before proceeding. With \code{trust = TRUE} each source string is
evaluated in a child of \code{\link[=globalenv]{globalenv()}} (not in the bayesgrove namespace)
and a warning notes that closures over the original environment are not
restored.
}
