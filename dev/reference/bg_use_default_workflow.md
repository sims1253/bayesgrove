# Activate the built-in starter workflow

Activate the built-in starter workflow

## Usage

``` r
bg_use_default_workflow(project)
```

## Arguments

- project:

  A `bg_handle`.

## Value

The updated `bg_handle`.
