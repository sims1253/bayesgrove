# Read the persisted goal registry

Read the persisted goal registry

## Usage

``` r
bg_read_goal_registry(project)
```

## Arguments

- project:

  A `bg_handle`.

## Value

A plain-data goal registry document.
